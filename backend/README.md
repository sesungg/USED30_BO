# used30 backend

`backend/` is a minimal Spring Boot service scaffold for this repository.

## Stack

- Java 17
- Spring Boot 3.5.13
- Gradle

## Run

If Gradle is installed locally:

```bash
cd backend
gradle bootRun
```

Then open `http://localhost:8080/api/health`.

## Test

```bash
cd backend
gradle test
```
